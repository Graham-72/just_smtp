name: Just SMTP
description: Send site emails through an SMTP server of your choice.
package: Mail
type: module
core: 8.x
dependencies:
  - libraries:libraries

# configure = admin/config/system/just_smtp
# files[] = just_smtp.mail.inc
